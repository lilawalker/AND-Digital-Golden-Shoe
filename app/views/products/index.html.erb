<div class="mt-12 lg:mt-8 flex">
  <select class="filter text-4xl lg:text-lg text-gray-700 rounded-lg border-2 border-gray-400 mr-8 w-1/3" name="category">
    <option selected value>category</option>
    <%= options_for_select(@categories, selected: params[:category]) %>
  </select>

  <select class="filter text-4xl lg:text-lg text-gray-700 rounded-lg border-2 border-gray-400 mr-8 w-1/3" name="style">
    <option selected value>style</option>
    <%= options_for_select(@styles, selected: params[:style]) %>
  </select>

  <select class="filter text-4xl lg:text-lg text-gray-700 rounded-lg border-2 border-gray-400 w-1/3" name="colour">
    <option selected value>colour</option>
    <%= options_for_select(@colours, selected: params[:colour]) %>
  </select>
</div>

<section class="grid gap-6 grid-cols-2 lg:grid-cols-4">
  <% @products.each do |product| %>
      <%= render(product) %>
  <% end %>
</section>
